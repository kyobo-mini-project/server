<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kyobo.server.mapper.AdminMapper">

  <select id="findByCode" resultType="com.kyobo.server.entity.Admin">
    SELECT a.cinema_id,
           c.cinema_name,
           a.admin_password
    FROM admins a
    JOIN cinemas c ON c.cinema_id = a.cinema_id
    WHERE a.admin_code = #{code}
  </select>

</mapper>
